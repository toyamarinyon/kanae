name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: Kanae version, for example 0.1.0
        required: true
        default: 0.1.0
      prerelease:
        description: Mark the GitHub Release as a prerelease
        required: true
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  release:
    name: Build and publish release
    runs-on: macos-15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate inputs
        env:
          KANAE_VERSION: ${{ inputs.version }}
        run: |
          set -eu
          case "$KANAE_VERSION" in
            v*)
              echo "version must not include the leading v: $KANAE_VERSION" >&2
              exit 1
              ;;
            *[!0-9.]* | "" | *..* | .* | *.)
              echo "version must be a dotted numeric version like 0.1.0: $KANAE_VERSION" >&2
              exit 1
              ;;
          esac

      - name: Show toolchain
        run: |
          swift --version
          uname -a
          uname -m

      - name: Package release
        env:
          KANAE_VERSION: ${{ inputs.version }}
        run: sh scripts/package-release.sh

      - name: Verify release
        env:
          KANAE_VERSION: ${{ inputs.version }}
        run: sh scripts/verify-release.sh

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
          KANAE_VERSION: ${{ inputs.version }}
          KANAE_PRERELEASE: ${{ inputs.prerelease }}
        run: |
          set -eu
          tag="kanae-v$KANAE_VERSION"
          notes_file="$(mktemp)"
          cat > "$notes_file" <<EOF
          Release artifacts for Kanae $KANAE_VERSION.

          Install:

          \`\`\`sh
          curl -fsSL https://kanae.ultrahope.dev/install | sh
          \`\`\`
          EOF

          prerelease_args=""
          if [ "$KANAE_PRERELEASE" = "true" ]; then
            prerelease_args="--prerelease"
          fi

          gh release create "$tag" \
            dist/kanae-v"$KANAE_VERSION"-*.tar.gz \
            dist/kanae-v"$KANAE_VERSION"-*.tar.gz.sha256 \
            --title "Kanae $KANAE_VERSION" \
            --notes-file "$notes_file" \
            $prerelease_args
